n=int(input('enter n:'))
l=[]
for i in range(n):
    x=input().split()
    l=int(x[0])
    l.append(x[0])
    u=int(x[1])
    c=0
    for j in range(l+1,u):
    count=0
    for k in range(2,j):
      if j%k==0:
        c+=1
    if count==0:
        c+=1
print(c)
